### iosevka custom

The custom version of `iosevka` font. You can either build it by yourself ([`read the instructions`](./BUILD.md)) or download the built font from [`ttf-unhinted`](./ttf-unhinted) and install it