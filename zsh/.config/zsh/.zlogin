# Profile File! Runs on Login! Commands are executed here!
# Global Order: zshenv, zprofile, zshrc, zlogin, zlogout 

# <- Neofetch (if present) ->
# if [ -z "$TMUX" ] && [ -x "$(command -v neofetch)" ]; then
#    neofetch
# fi